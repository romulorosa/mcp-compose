global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: 'mcp-compose'
    environment: 'production'

# Alertmanager configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets: []
          # - alertmanager:9093

# Load rules once and periodically evaluate them
rule_files:
  # - "alert_rules.yml"

# Scrape configurations
scrape_configs:
  # MCP Compose metrics
  - job_name: 'mcp-compose'
    static_configs:
      - targets: ['mcp-compose:8000']
        labels:
          service: 'mcp-compose'
    metrics_path: '/metrics'
    scrape_interval: 10s
    scrape_timeout: 5s

  # Prometheus self-monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
        labels:
          service: 'prometheus'

  # Node Exporter (if installed)
  - job_name: 'node'
    static_configs:
      - targets: []
        # - node-exporter:9100
        labels:
          service: 'node-exporter'
